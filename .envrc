layout_dir=$(direnv_layout_dir)
if ! mkdir -p "$layout_dir" 2>/dev/null || ! touch "$layout_dir/.tau-write-probe" 2>/dev/null; then
  log_status "read-only direnv layout; skipping nix-direnv"
  return 0
fi
rm -f "$layout_dir/.tau-write-probe"

use flake
